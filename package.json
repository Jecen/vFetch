{
  "name": "venus-fetch",
  "version": "1.0.0",
  "description": "esay to use fetch lib",
  "main": "dist/vFetch.min.js",
  "scripts": {
    "watch": "babel -d dist/ src5 --watch",
    "build": "babel -d dist/ src/ && uglifyjs ./dist/vFetch.js -o ./dist/vFetch.min.js",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/Jecen/vFetch.git"
  },
  "keywords": [
    "fetch",
    "ajax"
  ],
  "author": "Jacen",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "uglify-js": "^3.4.9"
  }
}
